package com.techelevator.ordering;

import org.junit.After;
import org.junit.Assert;
import org.junit.Before;
import org.junit.Test;

public class CartTest {

    /*
    // An instance variable to reference the class we are testing
    private Cart cart;

    // @Before method that runs before each test
    @Before
    public void setup() {
        cart = new Cart();
    }

    // @After method that runs after every test
    @After
    public void cleanup() {
        // do code that cleans up or resets here
        System.out.println();
    }

    // test for...
    */
}
